<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p><%= title %></p>
    <a href="/adicionar">Novo item</a>
    <div class="lista">
      <ul>
        <% if(item != undefined){%>
          <% for(item of item){ %>
            <li>ID: <%= item.id %> | Nome: <%=item.nome %> | <a href="/<%=item.id%>/editar">Editar</a> | 
              <form action="/<%=item.id%>/excluir?_method=DELETE" method="post">
                <button type="submit">Deletar</button>
              </form>
            </li>
          <% } %>
        <% } %>
      </ul>
    </div>
  </body>
</html>
